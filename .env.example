# =============================================================================
# Configuration du serveur
# =============================================================================
# Port sur lequel nginx écoute (défaut: 8080 pour compatibilité Cloud Run)
NGINX_PORT=8080

# =============================================================================
# Configuration MLflow (pour le versioning et le téléchargement des modèles)
# =============================================================================
# URI du serveur MLflow de tracking
MLFLOW_TRACKING_URI=http://localhost:5000

# URL du stockage S3/MinIO pour les artefacts MLflow
MLFLOW_S3_ENDPOINT_URL=http://localhost:9000

# Clés d'accès S3/MinIO
AWS_ACCESS_KEY_ID=minio_access_key
AWS_SECRET_ACCESS_KEY=minio_secret_key

# =============================================================================
# Configuration email (optionnel - pour les notifications)
# =============================================================================
# Serveur SMTP
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Identifiants email
SMTP_EMAIL=noreply@yourcompany.com
SMTP_PASSWORD=your_app_password
SMTP_FROM_ALIAS=Multi-Segmentation API

# Email de l'administrateur (destinataire des notifications)
ADMIN_EMAIL=admin@yourcompany.com

# =============================================================================
# Configuration de l'API
# =============================================================================
# URL de base de l'API (utilisée par Streamlit pour faire les appels)
# En développement local: http://localhost:8080/api
# En production: https://your-domain.com/api
MULTISEG_API_BASE_URL=http://localhost:8080/api
